# Required for AI-generated Chinese section summaries in deep reading reports.
ANTHROPIC_API_KEY=

# Optional: improves OpenAlex rate limits by joining the polite pool.
OPENALEX_EMAIL=

# Optional: Semantic Scholar API key for higher rate limits.
SEMANTIC_SCHOLAR_API_KEY=

# Optional: Unpaywall requires an email when enabled in future versions.
UNPAYWALL_EMAIL=
